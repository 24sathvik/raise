"use client"

import { useEffect } from "react"
import App from "../src/App"

export default function Page() {
  useEffect(() => {
    // Initialize any Vite-specific setup if needed
  }, [])

  return <App />
}
